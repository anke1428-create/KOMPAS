[project]
name = "kompas-agent"
version = "0.1.0"
description = "KOMPAS psychotherapeutisch verklaringsonderzoek agent"
requires-python = ">=3.11"
dependencies = [
  "openai-agents>=0.2.0",
  "fastapi>=0.115.0",
  "uvicorn>=0.30.0",
  "pydantic>=2.8.0",
  "python-docx>=1.1.2",
]

[project.scripts]
kompas-agent = "main:cli"
